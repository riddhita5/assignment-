import 'package:flutter/material.dart';

class PaymentCrashDemo extends StatefulWidget {
  const PaymentCrashDemo({super.key});

  @override
  State<PaymentCrashDemo> createState() => _PaymentCrashDemoState();
}

class _PaymentCrashDemoState extends State<PaymentCrashDemo> {
  double walletBalance = 500.0;
  String? lastTransactionId;

  // સ્યુડો ક્રેશ લોગ (ChatGPT ને પૂછવા માટે):
  /*
  Stack Trace:
  E/flutter ( 1234): [ERROR:flutter/runtime/dart_vm_initializer.cc(41)] Unhandled Exception: Null check operator used on a null value
  E/flutter ( 1234): #0      _PaymentCrashDemoState._processPayment (package:module_7/task_4/payment_crash_demo.dart:25:28)
  ...
  */

  void _processPayment() {
    try {
      // INTENTIONAL BUG: using '!' on a null variable to simulate a crash
      // ChatGPT Fix: Check if lastTransactionId is null before using it, or provide a fallback.
      print("Processing Transaction ID: " + lastTransactionId!.toUpperCase()); 

      setState(() {
        walletBalance -= 100;
      });
    } catch (e, stackTrace) {
      debugPrint("CRASH LOG CAPTURED:\n$e\n$stackTrace");
      
      showDialog(
        context: context,
        builder: (context) => AlertDialog(
          title: const Text('Payment Failed (Simulated Crash)'),
          content: Text('Error: $e\n\nFix applied: Added null safety check.'),
          actions: [
            TextButton(
              onPressed: () {
                // APPLYING FIX:
                setState(() => lastTransactionId = "TXN${DateTime.now().millisecondsSinceEpoch}");
                Navigator.pop(context);
              },
              child: const Text('Apply Fix & Retry'),
            ),
          ],
        ),
      );
    }
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Paytm Style Wallet'),
        backgroundColor: Colors.blue[900],
      ),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            const Icon(Icons.account_balance_wallet, size: 100, color: Colors.blue),
            const SizedBox(height: 20),
            Text('Balance: ₹$walletBalance', style: const TextStyle(fontSize: 30, fontWeight: FontWeight.bold)),
            const SizedBox(height: 40),
            ElevatedButton(
              onPressed: _processPayment,
              style: ElevatedButton.styleFrom(backgroundColor: Colors.blue[900], foregroundColor: Colors.white),
              child: const Text('PAY ₹100'),
            ),
          ],
        ),
      ),
    );
  }
}
